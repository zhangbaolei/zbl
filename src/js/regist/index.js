

App.registView("competition",new App.View({
  el:"#competition",
  model:new App.Model()
}),true);

App.registView("competNoStart",new App.View({
  el:"#competNoStart",
  model:new App.Model()
}),false);

App.registView("account",new App.View({
  el:"#account",
  model:new App.Model()
}),false);

App.registView("ranking",new App.View({
  el:"#ranking",
  model:new App.Model()
}),false);

App.registView("competStart",new App.View({
  el:"#competStart",
  model:new App.Model()
}),false);

      